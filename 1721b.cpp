#include <iostream>
#include <vector>
#include <cmath>
#include <string>
#include <algorithm>

int main() // https://codeforces.com/problemset/problem/1721/B
{
    int t, n, m, sx, sy, d;
    std::cin >> t;
    for(int i = 0; i < t; i++)
    {
        std::cin >> n >> m >> sx >> sy >> d;
    }
}
